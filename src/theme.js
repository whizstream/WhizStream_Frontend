import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
            main: "#1a237e",
        },
        secondary: {
            main: "#f57c00",
        },
        background: {
            default: "#f5f5f5",
        },
        text: {
            primary: "#212121",
            secondary: "#757575",
        },
    },
});

export default theme;